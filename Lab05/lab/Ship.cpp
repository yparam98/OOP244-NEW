#include <iostream>
#include "Ship.h"

namespace sdds
{
    Ship::Ship()
    {
    }

    Ship::Ship(const char *incoming_type, Engine &incoming_arr, int incoming_size)
    {
    }

    explicit Ship::operator bool() const
    {
    }

    Ship &Ship::operator+=(Engine incoming_obj)
    {
    }

    bool Ship::operator<(double incoming_num) const
    {
    }

    void Ship::display() const
    {
    }

    bool operator<(double incoming_power, const Ship &incoming_obj)
    {
    }
} // namespace sdds